/**
 * The "Where we are" content, carried verbatim from v1's `lib/content`
 * (`content.status`), scoped to exactly the entry Status.tsx needs.
 *
 * ⚠️ UNVERIFIED. The stage statements below are v1's words, including the
 * "First pilot in 2026" line, which predates the 2027 season used elsewhere
 * on this site. They have NOT been corrected here — see the merge report for
 * the full verification checklist.
 */

import status from "../../docs/content/status.json";

export default status;
