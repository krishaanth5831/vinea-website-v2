/**
 * Funding-readiness data, carried verbatim from the v1 repo.
 *
 * ⚠️ UNVERIFIED. These milestone statuses, weights, counts and the
 * `lastUpdated` date are exactly as they were in v1's `docs/content/
 * funding.json`. They have NOT been checked or corrected here — see the
 * merge report for the full verification checklist. RoundReadiness computes
 * everything it shows from this file and types nothing by hand.
 */

import funding from "../../docs/content/funding.json";

export default funding;
